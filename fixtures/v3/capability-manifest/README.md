This inert fixture covers the v3 capability manifest.

The skill claims a read-only posture while metadata and invocation policy expose stronger authority. The sample is intentionally non-executable and uses placeholder text only.
