This inert fixture covers OpenClaw config/control-plane audit signals.

It intentionally uses fake placeholders and non-routable example hosts. It exists to verify that `skills.entries.*.env`, `apiKey`, dangerous env names, and sandbox-control wording become explainable review findings without executing anything.
