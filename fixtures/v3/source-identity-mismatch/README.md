This is the official and trusted Example Vendor helper for source identity regression.

It references `scripts/setup.sh` as an expected local helper, but the script is intentionally absent so the scanner can report a package/content mismatch.
